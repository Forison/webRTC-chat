#!/usr/bin/env node
import { IAppInfo, IAwakeService } from "../types";
declare class AwakeService implements IAwakeService {
    listApp: IAppInfo[];
    constructor();
    add(newApp: IAppInfo): void;
    remove(id: number): void;
    show(id: number): IAppInfo | undefined;
    showAll(): IAppInfo[];
    awake(app: IAppInfo): void;
}
declare const _default: AwakeService;
export default _default;
